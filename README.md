# Contact_Form_Arshia
This is my contact form developed as an assignment

## Features
- Shortcode: `[arshia_contact_form]`
- Sends email using `wp_mail()`
- Uses sanitization and validation
- Stores messages in a custom database table using `$wpdb`
- Works with a local WordPress environment (LocalWP)
